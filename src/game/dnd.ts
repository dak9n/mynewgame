/**
 * Метки перетаскивания. Общие для сумки и панели быстрого доступа: без общего
 * места одна сторона клала бы данные под одним именем, а другая искала под
 * другим, и перетаскивание молча не работало бы.
 *
 * Имена обязаны быть в нижнем регистре — браузер приводит типы dataTransfer
 * к нему сам, и 'text/x-Item' потом не нашёлся бы.
 */
export const DND = {
  /** Вид предмета, который тащат из сумки. */
  item: 'text/x-item',
  /** Номер ячейки, которую тащат внутри панели. */
  hotbar: 'text/x-hotbar',
} as const;
