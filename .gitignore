# зависимости и сборка
node_modules/
dist/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
Desktop.ini

# Tiled
*.tmx.autosave
*.tmj.autosave
*.tiled-session

# Photoshop
*.psb~
~*.psd

# бэкапы карты и временный файл записи
.map-backups/

# Исходники эльфа — 46 МБ иллюстраций по 2000x1050, из которых игра не читает
# ничего: ей нужен собранный атлас public/assets/hero.png (325 КБ).
# Лежат локально; если понадобится пересобрать атлас — tools/build-hero.py.
public/assets/hero/

# База аккаунтов: хеши паролей, не место в репозитории и не место в сборке.
.auth/

# Карта в работе (macos) — пока не готова, на GitHub не пушим.
public/assets/maps/macos.json
